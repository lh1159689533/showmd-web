name: CI
on:
  pull_request:
    branches: master
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Installing Dependencies
        run: npm install
      - name: Running Lint
        run: npm run eslint
  